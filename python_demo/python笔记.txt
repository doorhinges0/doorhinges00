
python 变量不需要提前声明，。 当给一个变量赋值的时候就创建了它，可能的是任何类型的对象,并且当变量出现在一个表达式中的时候，就会用其值替换它。在使用变量之前必须对其赋值。



字符串具有不可变性，对字符串的操作，都会创建一个新的字符串作为结果--- 因为字符串具有不可变性。


列表时可变的。大部分对列表的操作都会改变列表对象，而不是创建一个新的列表。


字典不是序列， 它们并不包含任何可靠的从左至右的顺序。这意味着如果我们建立一个字典，并将它打印出来，它的键也许会与我们输入时不同的顺序出现。


